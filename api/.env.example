# /opt/health-web-api/.env (VM, chmod 600, git 미추적)
DB_USER=healthweb
DB_PASSWORD=
DB_DSN=shindb_low
DB_WALLET_LOCATION=/opt/health-bot/wallet
DB_WALLET_PASSWORD=
ALLOW_ORIGIN=https://bemindfull21.github.io
# openssl rand -base64 48  로 각각 새로 생성 (공유 비번 재사용 금지)
JWT_SECRET=
INTERNAL_KEY=
